#include <stdio.h>

/**
 * main - Entry point of Tina program
 *
 * Return: Always 0
 */
int main(void)
{
	int number1 = 180;
	int number2 = 200;
	int sum = number1 + number2;

	printf("The sum of %d and %d is: %d\n", number1, number2, sum);

	return (0);
}
